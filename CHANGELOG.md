# Changelog

本技能的版本历史。版本号：`MAJOR.MINOR.PATCH`
- MAJOR：流程结构变更（环节增删 / 契约破坏性变更）
- MINOR：新增能力（脚本 / 环节 / 规范）
- PATCH：修复与文档

---

## [0.5.0] — 2026-09-20

**「可分发」版本。** 本版本的目标是：别人 clone 下来，放进任何遵循 Agent Skills 约定的 agent，零配置即可跑通。

### 新增

| 文件 / 能力 | 作用 |
|---|---|
| `scripts/state.py roots` | **路径解析自检**：打印每一层实际生效值与**来源**，配置没生效时能立刻看出被哪层覆盖 |
| `data/min-cards.json` | 技能自带的离线卡片集（20 张必需卡）—— 未配置外部知识库时的兜底 |
| `scripts/_config.py` | 统一配置解析（三级回退），替代全部硬编码路径 |
| `.gitignore` / `.gitattributes` / `LICENSE` / `README.md` | 开源分发配套 |
| `docs/exporting.md` | 重写为**事实版**：安装方式、配置优先级、兼容性结论、打包命令 |

### 修复

| # | 问题 | 影响 | 严重度 |
|---|---|---|---|
| 1 | **`SKILL.md` 硬编码本机绝对路径**（项目根目录、脚本目录、知识库目录） | 别人安装后，agent 会照着一个**不存在的路径**找项目 —— 技能表面可用、实际空转 | 🔴 高 |
| 2 | **`dep_check.py` 只认 `flashcards.json`** | 全新安装（走离线兜底）时直接报错退出 —— **违背"开箱即用"承诺** | 🔴 高 |
| 3 | `state.py` 模板路径仍指向 `templates/`（已改名 `assets/`） | 新建项目时模板找不到 | 🟡 中 |
| 4 | **文档与环境变量名不一致**（文档写 `AMZ_*`，代码是 `AMAZON_*`） | 按文档配置必然失败，且报错信息不指向真正原因 | 🟡 中 |
| 5 | `dep_check.py` 曾把「离线 20 张卡」报告成「外部完整知识库」 | **错误陈述** —— 与本技能「不许把无出处内容说成权威来源」的核心准则同类 | 🟡 中 |
| 6 | `coach_mode()` 忽略 `--coach-dir` 显式传参 | 命令行指定的目录不参与模式判定，报告与实际不符 | 🟡 中 |

### 变更

- 版本号进入 `SKILL.md` frontmatter；补 `license: MIT` 字段（Agent Skills 通用字段）
- `docs/exporting.md` 由「待讨论方案」改为「已落地事实」——原文档会让读者误以为问题尚未修复
- `_config.coach_mode(cli=None)`：模式判定改为**依据实际目录里的文件**，而非"配置了没有"

### 验证

```
四层自查   selfcheck 35 · crosscheck 21 · semantic_check 12 · report_check 9   全过
依赖检查   dep_check  过期 0 / 临近 0 / 有效 20 / 缺失 0
路径自检   state.py roots  正确报告来源层级
编译检查   13 个脚本全部可编译
分发审计   本机路径残留 0 · 敏感文件全部忽略 · 必备文件齐备
```

### 教训（值得单独记住）

> **「换机器可用」不能靠人工检查，要靠一个会自动跑的审计脚本。**
> 本次的硬编码路径里，有几处藏在 `SKILL.md` 的**正文说明**中 ——
> 脚本层早就参数化完毕，文档层却还在教 agent 去访问本机路径。
> **只审计代码、不审计文档，等于没审计。**

> **降级路径必须被真实执行过。** `dep_check.py` 的离线兜底逻辑写是写了，
> 但从未跑过 —— 一跑就发现它只认 `flashcards.json`。
> **"有兜底分支"和"兜底分支能跑通"是两回事。**

---

## [0.4.0] — 2026-09-20

首个完成「自建 + 实战验证 + 全链路复核」的版本。

### 新增

| 文件 | 作用 |
|---|---|
| `references/06-numeric-semantics.md` | 数值语义规范：币种 / 单位 / 量纲 / 假设的标注要求 |
| `scripts/semantic_check.py` | 数值语义校验 + 假设追踪（对应 ROADMAP 的 P0-1、P0-3） |
| `scripts/report_check.py` | 报告与数据结构一致性检查（P0-2） |
| `scripts/dep_check.py` | 外部依赖（费率卡）时效检查（P1-6） |
| `docs/getting-started.md` | 起步指引（P2-8） |
| `docs/roadmap.md` | 成熟度评估与改进路线图 |

### 修复

| # | 问题 | 影响 |
|---|---|---|
| 1 | **抓取未校验币种** —— 页面按访问来源地本地化，`CNY 66.88` 被当成 `$66.88` | 售价放大 6.7 倍，污染环节 05/09 的结论 |
| 2 | 交叉检查**缺基数级校验** → 21 项全过却没抓到币种错误 | 已补（`semantic_check.py`） |
| 3 | 费用项**漏「低价 FBA 减免」**（售价 <$10 适用） | 低估费用缓冲 |
| 4 | 4 个 stage 文件（06/07/08/10）缺「输入要求」章节 | 结构不一致 |
| 5 | 2 个自查脚本未收录进 SKILL.md | 使用者不知要跑 |
| 6 | `crosscheck.py` **硬编码旧价格 66.88** → KeyError | 已改为从数据读取 |

### 变更

- 环节 05 / 09 的 v1 报告归档为 `report-v1-deprecated.md`，新报告基于修正后的售价重写
- 环节 02 的价格字段补充 `currency` 与 `price_correction` 修正记录
- 环节 05 的假设字段结构化为 `is_assumption` 格式（含 basis / impact / how_to_verify）

### 已知问题与教训

1. **校验器本身也会误报** —— `semantic_check` 首版产生 63 项误报（连"标题字符数"都要求写单位）；
   `report_check` 首版从描述文本抓数字（`$20.12` 抓成 `20`），单环节虚报 60 处。
   **误报泛滥会让校验被忽略、失去价值。两条都已修，并把教训写进代码注释。**

2. **自洽 ≠ 正确** —— 这是本版本最重要的一条：
   56 项检查全过，却没抓到币种错误，因为所有环节用的是同一个错数字。
   **一致性检查发现不了「共同的错误前提」。**

3. **未完成的优化项**：见 `docs/roadmap.md` 的 P1-4（脚本测试）、P2-7（项目模板）、P2-9（批量处理）。

---

## [0.3.0] — 2026-09-20

### 新增
- 10 个环节定义（`stages/`）
- 6 份规则文档（`references/`）
- 状态管理、抓取、测算、自检等脚本

### 说明
本版本完成「从零搭出一套可跑通的流程」，但尚未经过实战验证与复核。
